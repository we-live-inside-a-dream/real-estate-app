{
  "name": "real-estate-app",
  "version": "1.0.0",
  "description": "A real estate application",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run start:api\" \"npm run start:client\" \"npm run start:socket\"",
    "start:api": "npm --prefix api start",
    "start:client": "npm --prefix client start",
    "start:socket": "npm --prefix socket start",
    "install": "npm install --prefix api && npm install --prefix client && npm install --prefix socket"
  },
  "devDependencies": {
    "concurrently": "^6.2.1"
  }
}
